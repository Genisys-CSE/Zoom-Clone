# Python / backend
backend/.venv/
backend/__pycache__/
backend/*.pyc
backend/zoom.db

# Node / frontend
frontend/node_modules/
frontend/.next/
frontend/out/
frontend/*.tsbuildinfo

# Env + secrets (OWNER_TOKEN, API URLs)
.env*
!.env.example

# Personal notes (never push — public repo gets README.md only)
PERSONAL-README.md

# Logs + OS
*.log
.DS_Store
